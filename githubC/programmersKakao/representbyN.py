#count = 0

possibleList = [1] 

def arithmetic(N, List) :
    if N + N not in possibleList :
        possibleList.append(N+N)
    if N * N not in possibleList :
        possibleList.append(N*N)

def solution(N, number):
    count = 0
    
   
    possibleList
    
    return answer
