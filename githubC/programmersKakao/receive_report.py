def solution(id_list, report, k):
    answer = []
    point = [] #two list for report and result
    report_dic = {}
    #dictionary
    for each in report:
        if each.split()[0] in report_dic:
            each.split()[0]
    
    return answer
